<h1>Editing item</h1>

<%= render "form", item: @item %>

<br>

<div>
  <%= link_to "Show this item", restaurant_item_path(@item.restaurant,@item) %> |
  <%= link_to "Back to items",restaurant_item_path(@item.restaurant) %>
</div>
